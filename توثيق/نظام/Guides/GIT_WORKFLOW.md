# 🌿 GIT_WORKFLOW - دليل سير عمل Git

> **⚠️ تعليمات:** هذا الدليل يوضح استراتيجية الفروع المعتمدة وكيفية استخدام Git و GitHub في المشروع. يجب على جميع المبرمجين الالتزام بهذه الخطوات.

---

## 1. استراتيجية الفروع (Branching Strategy)

نحن نعتمد استراتيجية **GitFlow المبسطة**. لدينا ثلاثة أنواع من الفروع:

| الفرع | الغرض | دورة الحياة |
| :--- | :--- | :--- |
| `main` | **النسخة المستقرة (Production):** يحتوي على الكود الذي تم اختباره ونشره. | طويل الأمد |
| `develop` | **فرع التطوير (Integration):** يحتوي على الميزات المكتملة التي تنتظر الاختبار والنشر. | طويل الأمد |
| `feature/*` | **فروع الميزات (Feature Branches):** فرع لكل ميزة جديدة أو إصلاح خطأ. | قصير الأمد (يُحذف بعد الدمج) |

---

## 2. سير العمل اليومي للمبرمج (Daily Workflow)

اتبع هذه الخطوات عند العمل على أي مهمة جديدة (مثلاً: بناء صفحة تسجيل الدخول).

### الخطوة 1: تحديث فرع `develop` المحلي

قبل البدء في أي عمل جديد، تأكد من أن نسختك المحلية من `develop` محدثة بآخر التغييرات من GitHub.

```bash
# انتقل إلى فرع develop
git checkout develop

# اسحب آخر التغييرات من الخادم
git pull origin develop
```

### الخطوة 2: إنشاء فرع جديد للميزة

أنشئ فرعًا جديدًا من `develop` لمهمتك الحالية. اسم الفرع يجب أن يكون وصفيًا.

```bash
# أنشئ فرعًا جديدًا وانتقل إليه
# مثال: git checkout -b feature/login-page
git checkout -b feature/your-feature-name
```

### الخطوة 3: كتابة الكود والت提交 (Commit)

- ابدأ بكتابة الكود الخاص بالميزة في هذا الفرع الجديد.
- قم بعمل `commit` بشكل متكرر. كل `commit` يجب أن يمثل تغييرًا منطقيًا صغيرًا.
- **اكتب رسائل `commit` واضحة ومفيدة.**

  ```bash
  # أضف الملفات التي تم تغييرها
  git add .

  # قم بعمل commit مع رسالة واضحة
  # مثال: git commit -m "feat: Add email and password fields to login form"
  git commit -m "type: Brief description of your change"
  ```

  **أنواع الـ `commit`:**
  - `feat:` (ميزة جديدة)
  - `fix:` (إصلاح خطأ)
  - `docs:` (تغييرات في التوثيق)
  - `style:` (تنسيق الكود)
  - `refactor:` (إعادة هيكلة الكود بدون تغيير وظيفي)
  - `test:` (إضافة اختبارات)

### الخطوة 4: رفع الفرع إلى GitHub

عندما تكون مستعدًا لمشاركة عملك أو أخذ نسخة احتياطية منه، ارفع فرعك إلى GitHub.

```bash
# ارفع الفرع إلى الخادم (origin)
git push -u origin feature/your-feature-name
```

### الخطوة 5: إنشاء طلب سحب (Pull Request)

عندما تنتهي من الميزة بالكامل وتكون جاهزة للمراجعة والدمج:

1. اذهب إلى صفحة المستودع على GitHub.
2. ستجد زرًا لإنشاء **Pull Request (PR)** من فرعك الجديد.
3. قم بإنشاء الـ PR. الهدف هو دمج فرع الميزة (`feature/your-feature-name`) في فرع `develop`.
4. **اكتب وصفًا واضحًا للـ PR:** ماذا فعلت؟ كيف يمكن للمراجع اختباره؟
5. **عين مراجعًا (Reviewer):** عين المبرمج الآخر في الفريق لمراجعة الكود الخاص بك.

### الخطوة 6: مراجعة الكود والدمج (Code Review & Merge)

- سيقوم المراجع بفحص الكود، ترك تعليقات، وطلب تغييرات إذا لزم الأمر.
- قم بإجراء التغييرات المطلوبة.
- بمجرد الموافقة على الـ PR، سيتم دمجه في فرع `develop`.
- **احذف فرع الميزة** بعد الدمج للحفاظ على نظافة المستودع.

---

## 3. قواعد ذهبية

- **❌ لا تعمل مباشرة على `main` أو `develop`.**
- **✅ أنشئ فرعًا لكل مهمة.**
- **✅ اكتب رسائل `commit` واضحة.**
- **✅ قم بمراجعة كود زميلك قبل الدمج.**
- **✅ احذف الفروع بعد دمجها.**
