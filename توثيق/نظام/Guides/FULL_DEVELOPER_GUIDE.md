# 📖 الدليل الشامل للمبرمجين: بناء مشروع S-ACM من الصفر

هذا الدليل هو خارطة الطريق الكاملة لبناء المشروع خطوة بخطوة باستخدام Django.

---

## المرحلة 0: إعداد المشروع

1.  **إنشاء المشروع:**
    ```bash
    django-admin startproject sacm_project .
    ```
    (النقطة `.` في النهاية مهمة لمنع إنشاء مجلد إضافي)

2.  **إعداد ملف `.env`:**
    - أنشئ ملفًا جديدًا باسم `.env` في المجلد الرئيسي.
    - أضف فيه المعلومات الحساسة:
      ```
      SECRET_KEY='your-secret-key-here'
      DEBUG=True
      DB_NAME='sacm_db'
      DB_USER='postgres'
      DB_PASSWORD='your-postgres-password'
      DB_HOST='localhost'
      DB_PORT='5432'
      GEMINI_API_KEY='your-gemini-key-here'
      ```

3.  **تعديل `settings.py`:**
    - استورد مكتبة `os` و `dotenv` في الأعلى.
    - قم بتحميل المتغيرات من `.env`.
    - استبدل القيم الحساسة بالمتغيرات الجديدة.

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"django settings.py environment variables"`

4.  **تشغيل الخادم لأول مرة:**
    ```bash
    python manage.py migrate
    python manage.py runserver
    ```
    - افتح المتصفح على `http://127.0.0.1:8000/` للتأكد أن كل شيء يعمل.

---

## المرحلة 1: بناء نماذج قاعدة البيانات (Models)

سنقوم بإنشاء تطبيق جديد لإدارة المستخدمين والمقررات.

1.  **إنشاء تطبيق `core`:**
    ```bash
    python manage.py startapp core
    ```

2.  **إضافة `core` إلى `INSTALLED_APPS`** في `settings.py`.

3.  **كتابة النماذج في `core/models.py`:**
    - بناءً على التحليل من ملف PDF، سنقوم بترجمة الجداول إلى نماذج Django.

    ```python
    from django.db import models
    from django.contrib.auth.models import AbstractUser

    class User(AbstractUser):
        # Django's built-in user model has username, email, password, etc.
        # We can extend it here if needed
        pass

    class Department(models.Model):
        name = models.CharField(max_length=100)
        description = models.TextField(blank=True, null=True)

    # ... and so on for all other tables
    ```

4.  **تطبيق التغييرات على قاعدة البيانات:**
    ```bash
    python manage.py makemigrations
    python manage.py migrate
    ```

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"django models tutorial"`

---

## المرحلة 2: لوحة تحكم المسؤول (Admin Panel)

لتسهيل إدارة البيانات، سنضيف النماذج إلى لوحة تحكم Django.

1.  **تعديل `core/admin.py`:**

    ```python
    from django.contrib import admin
    from .models import User, Department, Course # import all models

    admin.site.register(User)
    admin.site.register(Department)
    admin.site.register(Course)
    ```

2.  **إنشاء مستخدم مسؤول:**
    ```bash
    python manage.py createsuperuser
    ```
    - أدخل اسم المستخدم والبريد الإلكتروني وكلمة المرور.

3.  **استكشاف لوحة التحكم:**
    - اذهب إلى `http://127.0.0.1:8000/admin/` وقم بتسجيل الدخول.

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"django admin panel tutorial"`

---

## المرحلة 3: بناء الواجهات (Views & Templates)

هنا سنبني الصفحات التي يراها المستخدم.

1.  **إنشاء مجلد `templates`** داخل تطبيق `core`.
2.  **إنشاء ملفات HTML** داخل `core/templates` (مثلاً `login.html`, `home.html`).
3.  **كتابة الـ Views في `core/views.py`:**

    ```python
    from django.shortcuts import render

    def home_view(request):
        return render(request, 'home.html')

    def login_view(request):
        return render(request, 'login.html')
    ```

4.  **ربط الـ Views بالـ URLs في `sacm_project/urls.py`:**

    ```python
    from django.contrib import admin
    from django.urls import path
    from core.views import home_view, login_view

    urlpatterns = [
        path('admin/', admin.site.urls),
        path('', home_view, name='home'),
        path('login/', login_view, name='login'),
    ]
    ```

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"django views and templates tutorial"`

---

## المرحلة 4: نظام تسجيل الدخول والخروج

سنستخدم نظام المصادقة المدمج في Django.

1.  **تعديل `login.html`** لإضافة فورم تسجيل الدخول.
2.  **تعديل `login_view`** في `core/views.py` لمعالجة طلبات الدخول.
3.  **إضافة روابط تسجيل الخروج** وحماية الصفحات التي تتطلب تسجيل الدخول.

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"django authentication tutorial login logout"`

---

## المرحلة 5: بناء الميزات الأساسية (لكل دور)

- **واجهات الطالب:** عرض المقررات، تحميل الملفات.
- **واجهات المدرس:** رفع الملفات، إرسال الإشعارات.
- **واجهات المسؤول:** إدارة المستخدمين والمقررات.

لكل ميزة، اتبع الدورة:
1.  **Model:** هل تحتاج تعديل؟
2.  **URL:** أنشئ رابطًا جديدًا.
3.  **View:** اكتب منطق العرض.
4.  **Template:** صمم الواجهة.

---

## المرحلة 6: ربط ميزات الذكاء الاصطناعي (Gemini API)

1.  **إنشاء ملف `ai_utils.py`** داخل تطبيق `core`.
2.  **كتابة دالة للتواصل مع Gemini API** لتلخيص النصوص أو توليد الأسئلة.
3.  **استدعاء هذه الدالة** من الـ Views المناسبة.

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"how to use gemini api with python django"`

---

## المرحلة 7: النشر (Deployment)

بعد الانتهاء من التطوير، سنقوم بنشر المشروع على الإنترنت.

> **ملاحظة:** هذه مرحلة متقدمة، يمكننا التركيز عليها لاحقاً.

---

هذا الدليل يوفر لك خارطة طريق واضحة. تذكر استخدام **سير عمل Git** الموضح في الدليل الآخر عند العمل على كل ميزة.
