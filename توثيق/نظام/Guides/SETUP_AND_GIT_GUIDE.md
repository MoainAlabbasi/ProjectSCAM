# 🚀 دليل إعداد بيئة العمل واستخدام Git/GitHub (بدون Docker)

هذا الدليل يشرح بالتفصيل كيفية إعداد بيئة العمل على نظام Windows لتطوير مشروع S-ACM، بالإضافة إلى شرح سير العمل باستخدام Git و GitHub.

---

## الجزء الأول: 💻 إعداد بيئة العمل (Windows)

اتبع هذه الخطوات بالترتيب لإعداد جهازك.

### الخطوة 1: تثبيت Python

1.  اذهب إلى الموقع الرسمي لـ Python: **[python.org](https://www.python.org/downloads/)**
2.  حمّل آخر إصدار من Python (مثلاً Python 3.11).
3.  **مهم جداً:** أثناء التثبيت، تأكد من تحديد خيار **`Add Python to PATH`**.

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"how to install python on windows 11"`

### الخطوة 2: تثبيت Git

1.  اذهب إلى الموقع الرسمي لـ Git: **[git-scm.com](https://git-scm.com/download/win)**
2.  حمّل النسخة الخاصة بـ Windows.
3.  قم بالتثبيت مع الإعدادات الافتراضية.

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"how to install git on windows"`

### الخطوة 3: تثبيت PostgreSQL

1.  اذهب إلى الموقع الرسمي لـ PostgreSQL: **[postgresql.org/download/windows/](https://www.postgresql.org/download/windows/)**
2.  حمّل المثبت (Installer) المناسب لنظامك.
3.  أثناء التثبيت، سيطلب منك تعيين **كلمة مرور** للمستخدم `postgres`. **احفظ هذه الكلمة جيداً، ستحتاجها لاحقاً.**
4.  بعد التثبيت، ابحث عن برنامج `pgAdmin` وافتحه. هذا هو برنامج إدارة قواعد البيانات.

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"how to install postgresql and pgadmin on windows"`

### الخطوة 4: إنشاء قاعدة البيانات

1.  افتح `pgAdmin`.
2.  اتصل بالخادم باستخدام كلمة المرور التي عينتها.
3.  اضغط بزر الماوس الأيمن على `Databases` واختر `Create` -> `Database`.
4.  أدخل اسم قاعدة البيانات، مثلاً: `sacm_db`.

### الخطوة 5: تجهيز المشروع

1.  افتح Git Bash أو Command Prompt.
2.  أنشئ مجلدًا للمشروع: `mkdir S-ACM-Project && cd S-ACM-Project`
3.  أنشئ بيئة افتراضية لـ Python:
    ```bash
    python -m venv venv
    ```
4.  فعّل البيئة الافتراضية:
    ```bash
    .\venv\Scripts\activate
    ```
    (يجب أن يظهر `(venv)` قبل اسم المسار في الطرفية)

5.  ثبّت المكتبات الأساسية:
    ```bash
    pip install django psycopg2-binary python-dotenv
    ```

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"python virtual environment windows"`

---

## الجزء الثاني: 🌿 سير عمل Git و GitHub

هذا الجزء يشرح كيفية التعامل مع الكود بين أعضاء الفريق باستخدام استراتيجية الفروع.

### المفاهيم الأساسية

| الفرع | الغرض |
|:---|:---|
| **`main`** | النسخة النهائية المستقرة التي تعمل 100%. |
| **`develop`** | نسخة التطوير التي نجمع فيها كل الميزات الجديدة. |
| **`feature/*`** | فرع لكل ميزة جديدة (مثل `feature/login`). |

### سير العمل اليومي (للمبرمج)

اتبع هذه الخطوات **في كل مرة** تبدأ فيها العمل على ميزة جديدة.

**الخطوة 1: تحديث فرع `develop` المحلي**

تأكد دائماً أن نسختك المحلية من `develop` محدّثة بآخر التغييرات من الفريق.

```bash
# انتقل إلى فرع develop
git checkout develop

# اسحب آخر التغييرات من GitHub
git pull origin develop
```

**الخطوة 2: إنشاء فرع جديد للميزة**

لا تعمل أبداً على فرع `develop` مباشرة! أنشئ فرعاً جديداً باسم وصفي للميزة التي تعمل عليها.

```bash
# أنشئ فرع جديد وانتقل إليه
git checkout -b feature/user-login-page
```
(استبدل `user-login-page` باسم الميزة التي تعمل عليها)

**الخطوة 3: كتابة الكود**

الآن يمكنك كتابة الكود والتعديل على الملفات بأمان في هذا الفرع. عملك هنا لن يؤثر على أي شخص آخر في الفريق.

**الخطوة 4: حفظ التغييرات (Commit)**

بعد الانتهاء من جزء من العمل، قم بحفظ تغييراتك.

```bash
# أضف جميع الملفات المعدلة
git add .

# اكتب رسالة commit واضحة
git commit -m "feat: add login page html and css"
```

> **ملاحظة:** `feat:` تعني أنك أضفت ميزة جديدة. استخدم `fix:` لإصلاح خطأ، `docs:` لتحديث التوثيق.

**الخطوة 5: رفع الفرع إلى GitHub**

```bash
# ارفع فرعك الجديد إلى GitHub
git push origin feature/user-login-page
```

**الخطوة 6: إنشاء طلب سحب (Pull Request)**

1.  اذهب إلى صفحة مستودع `S-ACM-Project` على GitHub.
2.  ستجد رسالة صفراء تقترح عليك إنشاء **Pull Request** لفرعك الجديد. اضغط عليها.
3.  تأكد أنك تطلب دمج فرعك (`feature/user-login-page`) في فرع `develop`.
4.  اكتب عنواناً ووصفاً واضحاً للتغييرات التي قمت بها.
5.  عيّن أحد أعضاء الفريق لمراجعة الكود (Reviewer).
6.  اضغط `Create Pull Request`.

**ممنوع دمج أي كود في `develop` بدون مراجعة من شخص آخر!**

> 🎥 **فيديو تعليمي:** ابحث في يوتيوب عن: `"github pull request tutorial"`
