# 🔧 سجل المشاكل والحلول (Troubleshooting Log)

> **الغرض:** توثيق المشاكل التقنية التي واجهها الفريق أثناء تطوير المشروع وكيفية حلها، لتكون مرجعاً للفريق ولأي مطور مستقبلي.

---

## المرحلة 0: الإعداد والتجهيز (Phase 0)

### المشكلة 1: خطأ صلاحيات Git عند الرفع إلى GitHub

| البند | التفاصيل |
|:---|:---|
| **التاريخ** | 2026-01-09 |
| **المرحلة** | Phase 0 - الإعداد |
| **من واجهها** | معين العباسي |
| **البيئة** | Windows + Git |

**وصف المشكلة:**

عند محاولة رفع المشروع إلى GitHub باستخدام الأمر `git push`، ظهر الخطأ التالي:

```
Permission denied (403)
```

**سبب المشكلة:**

وجود اعتمادات (Credentials) قديمة محفوظة في Windows Credential Manager لمستخدم آخر (Nawal-Alrajwi)، مما منع Git من استخدام حساب المستخدم الجديد (MoainAlabbasi).

**الحل:**

تم حذف الاعتمادات القديمة قسرياً عبر الأمر التالي في PowerShell:

```powershell
cmdkey /delete:git:https://github.com
```

بعد تنفيذ الأمر، طلب النظام تسجيل الدخول بالحساب الجديد، وتم الرفع بنجاح:

```bash
git push -u origin main
```

**الدروس المستفادة:**

- عند العمل على جهاز مشترك أو جهاز استُخدم سابقاً مع حساب GitHub آخر، يجب التحقق من الاعتمادات المحفوظة.
- يمكن الوصول إلى Credential Manager في Windows عبر: `Control Panel > Credential Manager > Windows Credentials`.

---

### المشكلة 2: ترميز ملف requirements.txt

| البند | التفاصيل |
|:---|:---|
| **التاريخ** | 2026-01-09 |
| **المرحلة** | Phase 0 - الإعداد |
| **من واجهها** | (اكتُشفت أثناء المراجعة) |
| **البيئة** | Windows |

**وصف المشكلة:**

ملف `requirements.txt` تم حفظه بترميز UTF-16 بدلاً من UTF-8، مما قد يسبب مشاكل عند قراءته على أنظمة Linux أو بعض الأدوات.

**سبب المشكلة:**

بعض محررات النصوص في Windows (مثل Notepad) قد تحفظ الملفات بترميز UTF-16 افتراضياً.

**الحل المقترح:**

عند إنشاء ملفات نصية، استخدم أحد الخيارات التالية:
1. استخدام VS Code الذي يحفظ بترميز UTF-8 افتراضياً
2. في Notepad، اختر "Save As" ثم اختر "UTF-8" من قائمة Encoding
3. استخدام الأمر مباشرة في Terminal:
   ```bash
   pip freeze > requirements.txt
   ```

**الدروس المستفادة:**

- دائماً تحقق من ترميز الملفات النصية، خاصة عند العمل على Windows.
- VS Code هو الخيار الأفضل لتجنب مشاكل الترميز.

---

## قالب لتوثيق مشكلة جديدة

```markdown
### المشكلة X: [عنوان المشكلة]

| البند | التفاصيل |
|:---|:---|
| **التاريخ** | YYYY-MM-DD |
| **المرحلة** | Phase X - [اسم المرحلة] |
| **من واجهها** | [اسم العضو] |
| **البيئة** | [نظام التشغيل / الأداة] |

**وصف المشكلة:**

[وصف تفصيلي للمشكلة ورسالة الخطأ إن وجدت]

**سبب المشكلة:**

[تحليل سبب حدوث المشكلة]

**الحل:**

[الخطوات التي تم اتباعها لحل المشكلة]

**الدروس المستفادة:**

[ما يمكن تعلمه لتجنب المشكلة مستقبلاً]
```

---

## إحصائيات المشاكل

| المرحلة | عدد المشاكل | تم حلها | معلقة |
|:---|:---:|:---:|:---:|
| Phase 0 | 2 | 2 | 0 |
| Phase 1 | - | - | - |
| Phase 2 | - | - | - |
| Phase 3 | - | - | - |

---

**آخر تحديث:** 2026-01-09
