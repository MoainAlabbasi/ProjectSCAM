# 👨‍💻 الخطة البرمجية التفصيلية - المرحلة 1: بناء الأساس

هذه الخطة تغطي الأيام (1-3) من مرحلة التنفيذ الفعلي بعد إكمال المرحلة 0.

## 📅 اليوم 1: نماذج البيانات الأساسية (Core Models)

### الهدف: بناء هيكل قاعدة البيانات في Django.

**الخطوات:**
1.  إنشاء تطبيق جديد باسم `core` أو `academy`:
    ```bash
    python manage.py startapp academy
    ```
2.  تعريف النماذج في `academy/models.py`:
    - `Department` (القسم): اسم القسم، الوصف.
    - `Level` (المستوى): الأول، الثاني، إلخ.
    - `Course` (المادة): اسم المادة، الكود، القسم، المستوى.
3.  إجراء الهجرة (Migrations):
    ```bash
    python manage.py makemigrations
    python manage.py migrate
    ```

---

## 📅 اليوم 2: نظام المستخدمين والصلاحيات (Auth & Roles)

### الهدف: إعداد مستخدم مخصص يدعم الأدوار (طالب، مدرس، مسؤول).

**الخطوات:**
1.  تعديل `AbstractUser` لإضافة حقل `role`:
    ```python
    class User(AbstractUser):
        ROLE_CHOICES = (
            ('student', 'طالب'),
            ('teacher', 'مدرس'),
            ('admin', 'مسؤول'),
        )
        role = models.CharField(max_length=10, choices=ROLE_CHOICES)
        department = models.ForeignKey('Department', on_delete=models.SET_NULL, null=True)
    ```
2.  تحديث `settings.py` ليعرف Django المستخدم الجديد:
    ```python
    AUTH_USER_MODEL = 'academy.User'
    ```
3.  إعداد لوحة تحكم المدير (Admin Customization) لعرض النماذج الجديدة بشكل احترافي.

---

## 📅 اليوم 3: إدارة المحتوى الأولي (Content Management)

### الهدف: بناء نظام رفع الملفات (المحاضرات والمواد).

**الخطوات:**
1.  تعريف نموذج `Material` (المادة العلمية):
    - العنوان، الملف (FileField)، نوع الملف، المادة المرتبطة، المدرس الذي رفعها.
2.  إعداد إعدادات الوسائط (Media Settings) في `settings.py`:
    ```python
    MEDIA_URL = '/media/'
    MEDIA_ROOT = BASE_DIR / 'media'
    ```
3.  بناء واجهة بسيطة (View) لاختبار رفع ملف والتأكد من تخزينه بشكل صحيح.

---

## 💡 نصائح برمجية لهذه المرحلة:
- استخدم `verbose_name` في النماذج لتظهر الأسماء بالعربية في لوحة التحكم.
- تأكد من استخدام `on_delete=models.CASCADE` أو `SET_NULL` بحذر في العلاقات.
- التزم بتوثيق كل دالة (Docstrings) تشرح وظيفتها.

---
**تم تحديث هذه الخطة في 2026-01-09 لتناسب متطلبات المرحلة الحالية.**
